home-work-61: Розробка RESTful API з використанням Express

📘 Мета роботи
 - Розробити RESTful сервер на основі Node.js і Express.js з модульною архітектурою (MVC-патерн), який обробляє маршрути користувачів і статей.

------------------

⚙️ Використані технології

- Node.js

- Express.js

- ES Modules

npm як менеджер пакетів

------------------

🧱 Структура проекту
home-work-61/
│
├── controllers/
│   ├── homeController.js
│   ├── usersController.js
│   └── articlesController.js
│
├── middlewares/
│   ├── logRequests.js
│   └── basicAuth.js
│
├── routes/
│   ├── homeRoutes.js
│   ├── usersRoutes.js
│   └── articlesRoutes.js
│
├── app.js
├── package.json
└── README.md


---------------------

🧠 Опис архітектури (MVC-патерн)

Model (Модель): у цьому завданні не використовується (БД не підключено).

View (Представлення): не потрібне, оскільки сервер повертає текстові відповіді.

Controller (Контролери): містять логіку обробки запитів клієнтів.

Routes (Маршрути): відповідають за маршрутизацію запитів до відповідних контролерів.

Middlewares (Проміжні обробники): відповідають за логування та базову авторизацію.

🚦 Маршрути сервера
/ — головний маршрут

GET → Welcome to the Home Page!

/users

GET → List of users

POST → User {username} created successfully

/users/:userId

GET → Get user by Id route: {userId}

PUT → Put user by Id route: {userId}

DELETE → Delete user by Id route: {userId}

/articles

GET → List of articles

POST → Article {title} created successfully

/articles/:articleId

GET → Get article by Id route: {articleId}

PUT → Put article by Id route: {articleId}

DELETE → Delete article by Id route: {articleId}

🧩 Middlewares
logRequests.js

Логує кожен запит у консоль у форматі:

[GET] /users at 2025-11-08T13:08:18.000Z

basicAuth.js

Перевіряє заголовок Authorization.
Якщо він відсутній — повертає 401 Unauthorized.
Для тестування достатньо передавати:
Authorization: Basic test

🖥️ Конфігурація сервера

Сервер слухає порт 3000

Відповіді сервера — текстові

Усі маршрути обробляються через Express Router

📦 Інструкція з встановлення та запуску

Клонувати репозиторій:

git clone <посилання-на-репозиторій>
cd home-work-61


Встановити залежності:

npm install


Запустити сервер:

node app.js


Відкрити у браузері або виконати запити з консолі / Postman.

🧪 Перевірка роботи серверу
✅ 1. Перевірка головного маршруту
curl -Uri "http://localhost:3000/"


Результат:

StatusCode: 200
Content: Welcome to the Home Page!

✅ 2. Перевірка маршруту користувачів (GET)
curl -Uri "http://localhost:3000/users" -Headers @{Authorization="Basic test"}


Результат:

StatusCode: 200
Content: List of users

✅ 3. Перевірка створення користувача (POST)
curl -Uri "http://localhost:3000/users" `
     -Method POST `
     -Headers @{
       "Authorization"="Basic test"
       "Content-Type"="application/json"
     } `
     -Body '{"username":"vlad","password":"1234"}'


Результат:

StatusCode: 200
Content: User vlad created successfully

✅ 4. Перевірка маршруту статей (GET)
curl -Uri "http://localhost:3000/articles" -Headers @{Authorization="Basic test"}


Результат:

StatusCode: 200
Content: List of articles


📊 Очікуваний результат

Після запуску сервер:

коректно обробляє всі маршрути;

логування відображається у терміналі;

маршрути захищені базовою автентифікацією;

повертає текстові відповіді з кодом 200 OK.

----------------------------

🧾 Висновок

У ході роботи було розроблено RESTful API з використанням Express.js.
Проєкт має модульну структуру та реалізує принципи MVC-архітектури.
Створено контролери для користувачів і статей, реалізовано middleware-функції логування та базової авторизації.
Сервер протестовано за допомогою curl — усі маршрути повертають очікувані відповіді.
